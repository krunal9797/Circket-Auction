import { Player, Team } from '../types';

// Pure empty dataset - all live data is loaded directly from Firebase Cloud Firestore
export const INITIAL_TEAMS: Team[] = [];

export const INITIAL_PLAYERS: Player[] = [];
